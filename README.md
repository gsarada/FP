# FP

Test sagemaker endpoint - aws sagemaker-runtime invoke-endpoint --endpoint-name fp-dev-embedding-endpoint --cli-binary-format raw-in-base64-out --content-type application/json --body "{\"inputs\": \"vectorize me\"}" --output json /dev/stdout

# Enterprise Grade

1. Scalability  - Horizontal scaling to meet demand with no downtime
2. Security - Best practice information security and least privilege
3. Monitoring - Best tooling for trackign activities
4. Guardrails - Check LLM inputs and outputs to protect against LLM weaknesses
5. Explainability - Understand the rationale for LLM outputs
6. Observability - Deep insights into LLM behavior as traces and spans